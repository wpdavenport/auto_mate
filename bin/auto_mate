#!/usr/bin/env ruby

auto_mate_path = File.expand_path('../../lib', __FILE__)
$LOAD_PATH.unshift(auto_mate_path) unless $LOAD_PATH.include?(auto_mate_path)

require 'auto_mate/cli'

AutoMate::Cli.new(ARGV, STDIN).run